# SpringBoot-book

1. Download this project as a zip file and import it in your Eclipse IDE or STS, if it is eclipse then it is must to have a STS(Spring tool suite)plugin

2. Right click the project and run as spring boot app.

3. I have given the application.properties file in the path (src/main/resources/) . Kindly configure your mysql properties there.

